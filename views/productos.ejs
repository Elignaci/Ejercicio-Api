<%- include('templates/header') %>
    <%- include('templates/nav') %>

        <body>
            <% if (productos.length > 0) { %>
                <% productos.forEach(({descripcion, nombre, disponible}) => { %>
    
                    <% if (disponible) { %>
                        <article>
                            <h2><%= nombre %></h2>
                            <p><%= descripcion %></p>
                        </article>
                        <% } %>
    
    
                <% }) %> <% } else { %>
                 <p>productos no disponibles</p>
    
            <%}%>
        </body>

      <%-include('templates/footer')  %>